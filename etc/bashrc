# The Alias for the command. You need to change the path to the maia executable
if [[ -e "$HOME/.bashrc" ]] ; then
   . "$HOME/.bashrc"
fi
maias() {
    export AIA_SESSION="$1"
}
if [[ -n "$MAIA_PS1" ]] ; then
    export PS1="$MAIA_PS1"
fi
